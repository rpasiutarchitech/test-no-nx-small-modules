import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Container5Component } from './<%= dasherize(name) %>-container5.component';

describe('<%= classify(name) %>Container5Component', () => {
  let component: <%= classify(name) %>Container5Component;
  let fixture: ComponentFixture<<%= classify(name) %>Container5Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%= classify(name) %>Container5Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%= classify(name) %>Container5Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
