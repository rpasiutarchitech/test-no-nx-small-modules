import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Container10Component } from './<%= dasherize(name) %>-container10.component';

describe('<%= classify(name) %>Container10Component', () => {
  let component: <%= classify(name) %>Container10Component;
  let fixture: ComponentFixture<<%= classify(name) %>Container10Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%= classify(name) %>Container10Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%= classify(name) %>Container10Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
