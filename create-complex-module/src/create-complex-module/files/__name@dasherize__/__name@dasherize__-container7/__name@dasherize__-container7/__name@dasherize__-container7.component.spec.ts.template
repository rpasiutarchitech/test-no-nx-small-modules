import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Container7Component } from './<%= dasherize(name) %>-container7.component';

describe('<%= classify(name) %>Container7Component', () => {
  let component: <%= classify(name) %>Container7Component;
  let fixture: ComponentFixture<<%= classify(name) %>Container7Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%= classify(name) %>Container7Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%= classify(name) %>Container7Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
