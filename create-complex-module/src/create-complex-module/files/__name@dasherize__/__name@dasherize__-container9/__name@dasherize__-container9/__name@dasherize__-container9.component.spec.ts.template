import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Container9Component } from './<%= dasherize(name) %>-container9.component';

describe('<%= classify(name) %>Container9Component', () => {
  let component: <%= classify(name) %>Container9Component;
  let fixture: ComponentFixture<<%= classify(name) %>Container9Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%= classify(name) %>Container9Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%= classify(name) %>Container9Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
