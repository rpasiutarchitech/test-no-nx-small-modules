import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Container6Component } from './<%= dasherize(name) %>-container6.component';

describe('<%= classify(name) %>Container6Component', () => {
  let component: <%= classify(name) %>Container6Component;
  let fixture: ComponentFixture<<%= classify(name) %>Container6Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%= classify(name) %>Container6Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%= classify(name) %>Container6Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
