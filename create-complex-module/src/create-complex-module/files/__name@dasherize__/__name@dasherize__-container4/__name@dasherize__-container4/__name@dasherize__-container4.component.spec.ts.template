import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Container4Component } from './<%= dasherize(name) %>-container4.component';

describe('<%= classify(name) %>Container4Component', () => {
  let component: <%= classify(name) %>Container4Component;
  let fixture: ComponentFixture<<%= classify(name) %>Container4Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ <%= classify(name) %>Container4Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%= classify(name) %>Container4Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
